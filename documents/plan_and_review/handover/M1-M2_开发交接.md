# M2 开发交接文档

> 生成日期：2026-04-08  
> 目的：记录当前进度和计划，方便新对话继续执行

---

## 一、当前状态

### M1 基础设施（✅ 已完成）
| 文件 | 状态 |
|------|------|
| `source/include/logger.hpp` | ✅ 完成 |
| `source/include/util.hpp` | ✅ 完成 |
| `source/config/server.conf` | ✅ 完成 |
| `source/tests/test_base.cpp` | ✅ 7 项测试全部通过 |
| `source/CMakeLists.txt` | ✅ 完成 |

### M2 数据库与用户模块（⏳ 未开始）
| Phase | 模块 | 状态 |
|-------|------|------|
| Phase 1 | db.hpp + test_db_pool.cpp | ⏳ 待实现 |
| Phase 2 | user_table.hpp + test_user_table.cpp | ⏳ 待实现 |
| Phase 3 | security.hpp + test_security.cpp | ⏳ 待实现 |
| Phase 4 | main.cpp + test_auth_api.cpp | ⏳ 待实现 |

---

## 二、关键设计决策（已确认）

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 认证方案 | **纯 JWT** | 无状态、易扩展、Phase 4 简化 |
| 密码加密 | **OpenSSL PBKDF2** | Rocky 9 自带，0 依赖风险 |
| 连接池超时 | **3 秒超时** | 防止并发场景死锁 |
| password_hash 长度 | **VARCHAR(256)** | PBKDF2 存储格式约 100~120 字符，留余量 |

---

## 三、待执行任务清单

### 前置准备（在新对话中首先执行）

1. **检查虚拟机环境**
   ```bash
   # 在虚拟机中运行
   systemctl status mysqld --no-pager | head -5
   mysql --version
   mysql -u root -p'yourpassword' -e 'SHOW DATABASES LIKE "gobang_db";'
   pkg-config --exists openssl && echo "openssl: OK" || echo "openssl: 缺失"
   pkg-config --exists mysqlclient && echo "mysqlclient: OK" || echo "mysqlclient: 缺失"
   ```

2. **如环境未就绪，需要先配置**
   - 安装 MySQL 并创建数据库 `gobang_db`
   - 创建用户表（见下方 SQL）

### Phase 1: db.hpp 数据库连接池

**创建文件**：
- `source/include/db.hpp`
- `source/tests/test_db_pool.cpp`

**CMakeLists.txt 修改**：
```cmake
# 添加依赖
find_package(PkgConfig REQUIRED)
pkg_check_modules(MYSQL REQUIRED mysqlclient)
include_directories(${MYSQL_INCLUDE_DIRS})

# 封装测试目标
function(add_gobang_test target_name source_file)
    add_executable(${target_name} ${source_file})
    target_link_libraries(${target_name}
        ${JSONCPP_LIBRARIES}
        ${MYSQL_LIBRARIES}
        Threads::Threads
    )
endfunction()

# 添加测试
add_gobang_test(test_db_pool tests/test_db_pool.cpp)
```

**测试用例**：
- [ ] 连接池初始化测试
- [ ] 获取/归还连接测试
- [ ] 并发获取连接测试（10 线程，池大小 5，验证 3 秒超时）

---

### Phase 2: user_table.hpp 用户数据访问

**创建文件**：
- `source/include/user_table.hpp`
- `source/tests/test_user_table.cpp`

**CMakeLists.txt**：
```cmake
add_gobang_test(test_user_table tests/test_user_table.cpp)
```

---

### Phase 3: security.hpp 密码加密和 JWT

**创建文件**：
- `source/include/security.hpp`
- `source/tests/test_security.cpp`
- `source/include/3rdparty/jwt-cpp/`（下载 jwt-cpp）

**jwt-cpp 来源**：
```bash
git clone https://github.com/Thalhammer/jwt-cpp.git
cp -r jwt-cpp/include/jwt-cpp source/include/3rdparty/
```

**CMakeLists.txt**：
```cmake
pkg_check_modules(OPENSSL REQUIRED openssl)
include_directories(${OPENSSL_INCLUDE_DIRS})
add_gobang_test(test_security tests/test_security.cpp)
```

---

### Phase 4: main.cpp HTTP API 整合

**创建文件**：
- `source/src/main.cpp`
- `source/tests/test_auth_api.cpp`

---

## 四、数据库初始化 SQL

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS gobang_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户表
USE gobang_db;

CREATE TABLE user (
    id INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(32) UNIQUE NOT NULL COMMENT '用户名',
    password_hash VARCHAR(256) NOT NULL COMMENT 'PBKDF2 密码哈希',
    score INT UNSIGNED DEFAULT 1500 COMMENT '天梯分数',
    total_count INT UNSIGNED DEFAULT 0 COMMENT '总场次',
    win_count INT UNSIGNED DEFAULT 0 COMMENT '胜场',
    status TINYINT DEFAULT 0 COMMENT '0-离线 1-大厅 2-房间',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_score (score),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 五、重要参考文档

| 文档 | 路径 |
|------|------|
| M2 开发计划 | `plan_and_review/M2_开发计划.md` |
| M1 复盘报告 | `plan_and_review/M1_复盘报告.md` |
| 项目总体规划 | `documents/project_plan_v2.2.md` |
| 同步流程 | `memory/sync_workflow.md` |

---

## 六、项目当前文件结构

```
OnlineGobangBattle/
├── plan_and_review/
│   ├── M1_复盘报告.md
│   ├── M2_开发计划.md          ← M2 详细计划
│   └── M2_开发交接.md          ← 本文件
├── documents/
│   ├── project_plan_v2.2.md
│   ├── project_alternatives.md
│   └── mysql.c                 ← MySQL C API 示例代码
├── source/
│   ├── CMakeLists.txt
│   ├── config.h.in
│   ├── config/server.conf
│   ├── include/
│   │   ├── logger.hpp
│   │   └── util.hpp
│   │   ├── db.hpp              ← M2 Phase 1 待创建
│   │   ├── user_table.hpp      ← M2 Phase 2 待创建
│   │   └── security.hpp        ← M2 Phase 3 待创建
│   └── tests/
│       ├── test_base.cpp
│       ├── test_db_pool.cpp    ← M2 Phase 1 待创建
│       ├── test_user_table.cpp ← M2 Phase 2 待创建
│       └── test_security.cpp   ← M2 Phase 3 待创建
└── memory/
    └── sync_workflow.md
```

---

## 七、下一步行动

在新对话中执行：

1. **首先**：运行环境检查指令，确认 MySQL、OpenSSL 已安装
2. **然后**：开始 Phase 1 - db.hpp 的实现和测试
3. **原则**：一个模块一个模块测试，通过后再进行下一个

---

*交接文档版本：v1.0*  
*下次对话时，把此文件内容提供给新的会话即可继续*
